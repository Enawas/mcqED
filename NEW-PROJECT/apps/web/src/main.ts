/**
 * Purpose: Entry point for the QCM web application. Registers global styles
 * and bootstraps the router and shell components.
 * Inputs: None (executed on load).
 * Outputs: Bootstraps the application UI.
 * Events: None.
 * Errors: None.
 */
import './router';
// TODO: register global design-system components and mount root component.