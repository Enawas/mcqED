{
  "name": "@packages/db",
  "private": true,
  "scripts": {
    "generate": "drizzle-kit generate:pg --config=drizzle.config.ts",
    "migrate:up": "drizzle-kit up --config=drizzle.config.ts",
    "migrate:down": "drizzle-kit down --config=drizzle.config.ts",
    "lint": "echo 'No lint configured yet for db package'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No DB unit tests yet'"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.1",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.8",
    "typescript": "^5.3.3"
  }
}